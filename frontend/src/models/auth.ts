/**
 * "Short" User useful for checking credentials and displaying the navbar.
 */
export interface Auth {
    id: string;
    firstName: string;
    lastName: string;
    promotion: number;
    isStaff: boolean;
}