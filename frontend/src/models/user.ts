/**
 * "Short" User useful for checking credentials and displaying the navbar.
 */
export interface User {
    id: string;
    firstName: string;
    lastName: string;
    promotion: string;
    isStaff: boolean;
}
