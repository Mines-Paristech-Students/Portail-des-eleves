import React, { useContext } from "react";
import { ToastContext, ToastLevel } from "./utils/Toast";
import Button from "react-bootstrap/Button";

/*export const Test = ({}: {}) => {
    const { sendToast } = useContext(ToastContext);

    return (
        <>
            <Button
                type="button"
                onClick={() => sendToast("piche", ToastLevel.Info)}
            />
            <Button type="button" onClick={() => sendToast("poche")} />
            <Button
                type="button"
                onClick={() => sendToast("piche", ToastLevel.Warning)}
            />
        </>
    );
};*/
